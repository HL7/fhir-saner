"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const FshEntity_1 = require("./FshEntity");
class FshRatio extends FshEntity_1.FshEntity {
    constructor(numerator, denominator) {
        super();
        this.numerator = numerator;
        this.denominator = denominator;
    }
    toString() {
        return `${this.numerator.toString()} : ${this.denominator.toString()}`;
    }
    toFHIRRatio() {
        const ratio = {};
        if (this.numerator) {
            ratio.numerator = this.numerator.toFHIRQuantity();
        }
        if (this.denominator) {
            ratio.denominator = this.denominator.toFHIRQuantity();
        }
        return ratio;
    }
    equals(other) {
        return this.numerator.equals(other.numerator) && this.denominator.equals(other.denominator);
    }
}
exports.FshRatio = FshRatio;
//# sourceMappingURL=FshRatio.js.map