import { Rule } from './rules/Rule';
import { FshEntity } from './FshEntity';
export declare class Extension extends FshEntity {
    name: string;
    id: string;
    parent: string;
    title?: string;
    description?: string;
    mixins?: string[];
    rules: Rule[];
    constructor(name: string);
}
