/**
 * Abstract class to contain elements common to all FSH types.
 */
export declare abstract class FshEntity {
    readonly sourceInfo: SourceInfo;
    withLocation(location: TextLocation | [number, number, number, number]): this;
    withFile(file: string): this;
    withAppliedLocation(location: TextLocation | [number, number, number, number]): this;
    withAppliedFile(file: string): this;
}
export declare type SourceInfo = {
    file?: string;
    location?: TextLocation;
    appliedFile?: string;
    appliedLocation?: TextLocation;
};
export declare type TextLocation = {
    startLine: number;
    startColumn: number;
    endLine: number;
    endColumn: number;
};
