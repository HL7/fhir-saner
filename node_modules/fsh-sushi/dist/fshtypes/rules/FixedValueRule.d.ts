import { Rule } from './Rule';
import { FshCode, FshQuantity, FshRatio, FshReference } from '../index';
import { InstanceDefinition } from '../../fhirtypes';
export declare type FixedValueType = boolean | number | string | FshCode | FshQuantity | FshRatio | FshReference | InstanceDefinition;
export declare class FixedValueRule extends Rule {
    fixedValue: FixedValueType;
    exactly: boolean;
    units: boolean;
    isResource: boolean;
    constructor(path: string);
}
