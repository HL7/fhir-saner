import { FshEntity } from '.';
import { MappingRule } from './rules/MappingRule';
/**
 * The Mapping class is used to contain mapping info for SDs
 */
export declare class Mapping extends FshEntity {
    name: string;
    id: string;
    source?: string;
    target?: string;
    description?: string;
    title?: string;
    rules: MappingRule[];
    constructor(name: string);
}
