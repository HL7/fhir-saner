"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const FshEntity_1 = require("./FshEntity");
class FshReference extends FshEntity_1.FshEntity {
    constructor(reference, display) {
        super();
        this.reference = reference;
        this.display = display;
    }
    toString() {
        return `Reference(${this.reference})${this.display ? ` "${this.display}"` : ''}`;
    }
    toFHIRReference() {
        const reference = {
            reference: this.reference
        };
        if (this.display) {
            reference.display = this.display;
        }
        return reference;
    }
    equals(other, ignoreOtherDisplay = false) {
        return (this.reference === other.reference &&
            (this.display == other.display || (ignoreOtherDisplay && other.display == null)));
    }
}
exports.FshReference = FshReference;
//# sourceMappingURL=FshReference.js.map