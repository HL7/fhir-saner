import { Annotated } from './Annotated';
export declare class InvalidRangeValueError extends Error implements Annotated {
    low: number;
    high: number;
    specReferences: string[];
    constructor(low: number, high: number);
}
