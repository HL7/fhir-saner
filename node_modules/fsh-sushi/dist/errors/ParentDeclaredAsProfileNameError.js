"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class ParentDeclaredAsProfileNameError extends Error {
    constructor(name, sourceInfo, fhirResourceUrl) {
        super(`Profile "${name}" cannot declare itself as a Parent. ${fhirResourceUrl
            ? `It looks like the parent is an external resource; use its URL (e.g., ${fhirResourceUrl}).`
            : ''}`);
        this.name = name;
        this.sourceInfo = sourceInfo;
        this.fhirResourceUrl = fhirResourceUrl;
    }
}
exports.ParentDeclaredAsProfileNameError = ParentDeclaredAsProfileNameError;
//# sourceMappingURL=ParentDeclaredAsProfileNameError.js.map