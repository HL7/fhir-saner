import { WithSource } from './WithSource';
import { SourceInfo } from '../fshtypes';
export declare class ParentDeclaredAsProfileNameError extends Error implements WithSource {
    name: string;
    sourceInfo: SourceInfo;
    fhirResourceUrl: string;
    constructor(name: string, sourceInfo: SourceInfo, fhirResourceUrl: string);
}
