import { Annotated } from './Annotated';
export declare class InvalidCardinalityError extends Error implements Annotated {
    min: number;
    max: string;
    specReferences: string[];
    constructor(min: number, max: string);
}
