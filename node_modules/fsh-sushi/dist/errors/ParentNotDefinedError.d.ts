import { Annotated } from './Annotated';
import { WithSource } from './WithSource';
import { SourceInfo } from '../fshtypes';
export declare class ParentNotDefinedError extends Error implements Annotated, WithSource {
    childName: string;
    parentName: string;
    sourceInfo: SourceInfo;
    specReferences: string[];
    constructor(childName: string, parentName: string, sourceInfo: SourceInfo);
}
