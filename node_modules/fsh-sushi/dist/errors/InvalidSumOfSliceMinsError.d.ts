import { Annotated } from './Annotated';
export declare class InvalidSumOfSliceMinsError extends Error implements Annotated {
    sumMins: number;
    max: string;
    slicedElementId: string;
    specReferences: string[];
    constructor(sumMins: number, max: string, slicedElementId: string);
}
