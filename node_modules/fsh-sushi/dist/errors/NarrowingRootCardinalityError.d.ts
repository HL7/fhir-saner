import { Annotated } from './Annotated';
export declare class NarrowingRootCardinalityError extends Error implements Annotated {
    rootElement: string;
    existingSlice: string;
    newMin: number;
    newMax: string;
    sliceMin: number;
    sliceMax: string;
    specReferences: string[];
    constructor(rootElement: string, existingSlice: string, newMin: number, newMax: string, sliceMin: number, sliceMax: string);
}
