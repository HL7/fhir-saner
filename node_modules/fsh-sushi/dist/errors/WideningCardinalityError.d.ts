import { Annotated } from './Annotated';
export declare class WideningCardinalityError extends Error implements Annotated {
    originalMin: number;
    originalMax: string;
    newMin: number;
    newMax: string;
    specReferences: string[];
    constructor(originalMin: number, originalMax: string, newMin: number, newMax: string);
}
