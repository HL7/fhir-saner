import { ValueSet } from '../fhirtypes';
import { FSHTank } from '../import/FSHTank';
import { FshValueSet } from '../fshtypes';
import { Package } from '.';
import { MasterFisher } from '../utils';
export declare class ValueSetExporter {
    private readonly tank;
    private pkg;
    private fisher;
    constructor(tank: FSHTank, pkg: Package, fisher: MasterFisher);
    private setMetadata;
    private setCompose;
    private setCaretRules;
    private filterValueToString;
    export(): Package;
    exportValueSet(fshDefinition: FshValueSet): ValueSet;
}
